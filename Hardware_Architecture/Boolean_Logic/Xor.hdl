/**
 * Exclusive-or gate:
 * if ((a and Not(b)) or (Not(a) and b)) out = 1, else out = 0
 */
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a, out=W1);
    Not(in=b, out=W2);
    And(a=W1, b=b, out=W3);
    And(a=a, b=W2, out=W4);
    Or(a=W3, b=W4, out=out); 
}
